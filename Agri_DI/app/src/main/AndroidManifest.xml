<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.companies.patil.agri_di">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".signup" />
        <activity
            android:name=".main_disp"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".data_collect" />
        <activity android:name=".Data_collect_test"></activity>
        <activity android:name=".temp_data" />
        <activity android:name=".LandRent" />
        <activity android:name=".DailyPlantAmount" />
        <activity android:name=".Give_Rent" />
        <activity android:name=".Show_Rent" />
        <activity android:name=".rent_show_details" />
        <activity android:name=".storage_plants" />
        <activity android:name=".selectStoragePlace"></activity>
        <activity android:name=".servayresponse" />
        <activity android:name=".Home">
        </activity>
    </application>

</manifest>